-- when to use implicit typing
	
	anonymous types

		var someType = new {a = "Something", b = 100}

	LINQ queries (special case of anonymous types)



-- issues caused by implicit typing

	var myFloat = 100.0; 	--> This is now up to programmers to enforce
	var money = 9.99; 		--> Let's hope that doesn't become a double!